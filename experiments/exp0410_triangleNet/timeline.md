# Timeline — exp0410_triangleNet

## 2026-04-09

### 项目启动
- 实现标量节点版本的 TMN（每个节点是一个标量，每条边是一个标量权重）
- 移除 `d_model` 参数，参数量从 ~22k 降到 ~22（L=3）

### 性能优化
- **问题**：TMN forward 比 MLP 慢 18x
- **原因**：Python 字符串操作 + `ParameterDict` 查找 + `sum(generator)` 循环
- **优化过程**：
  1. 字符串 → 整数索引：18x → 17x
  2. `ParameterList` → 单张量 `ew`：17x → 17x（无明显改善）
  3. 向量化聚合（`cat + sum`）：17x → 13x
  4. `scatter_add` 按层批量：13x → 10x
  5. `hist` 增量拼接（转置布局）：10x → 9x
  6. 消除 `unsqueeze`（预计算 `dst_rows_0`）：9x → **8.4x**
- **剩余瓶颈**：Python for 循环遍历层级（~15 次）+ 每层多次 PyTorch dispatch

### 权重初始化修复
- **问题**：底边中间节点（如 `(4,4)`）的边权和偏置始终为 0
- **原因**：固定 `std=0.1` 初始化导致长路径信号衰减（`0.1^4 = 0.0001`），ReLU 输出始终为 0（死亡神经元）
- **修复**：Kaiming init — 每条边的 `std = sqrt(2 / n_parents)`，保证 ReLU 后方差不衰减
- **结果**：底边节点偏置从 0 变为非零，节点开始接受梯度更新

### 可视化改进
- 新增参数演化曲线图：追踪底边所有节点的偏置和入边权重随 epoch 变化
- 输出 `weights.png`：左侧 TMN DAG + 右侧两条曲线（边权演化 + 偏置演化）

### 关键发现
- 底边节点 `(4,1)` 在约 2000 epoch 后从死亡状态复活（偏置从 0 飙升到 0.8）
- 底边节点 `(4,2), (4,3), (4,4)` 在 10000 epoch 后仍然≈0，判定为永久死亡
- **死亡不是永恒的**，但复活窗口主要在训练早期（权重还在大幅调整时）
- **死亡会传播**：上游节点死亡 → 下游节点收不到信号 → 也死亡

---

## 2026-04-10

### 3D TMN 实现完成

- **XYZ 坐标系统确立**：
  - `x` = 深度（里→外），范围 `1` 到 `depth`（所有层相同）
  - `y` = 位置（左→右），范围 `1` 到 `L-z+1`（正三角形结构）
  - `z` = 层（下→上），范围 `1` 到 `L`
  - 节点记法：`(x, y, z)` = `(深度，位置，层)`
  - 第 z 层有 `L-z+1` 个位置（z=1 底层有 L 个位置，z=L 顶层有 1 个位置）

- **连接规则确定**：
  | 类型 | 方向 | 规则 | 示例（depth=1, L=4） |
  |------|------|------|------------------------|
  | **层内连接** | Y（左→右） | 相邻位置全连接 | `(1,1,4) → (1,2,4)` |
  | **下→上连接** | Z+（垂直向上） | `(x,y,z) → (x,y,z+1)` | `(1,1,3) → (1,1,4)` |
  | **上→下连接** | Z-（斜向右下） | `(x,y,z) → (x,y+1,z-1)` | `(1,1,4) → (1,2,3)` |

- **深度扩展规则**（depth=d）：
  - 层内：相邻位置之间全连接 `d²` 条边
  - 跨层：旧版本是 `shared_x`，每个位置对位置只有 `d` 条边
  - 跨层增强版是 `full_x`，每个位置对位置有 `d²` 条边
  - 输入：连接到每层最左位置的所有深度神经元
  - 输出：从每层最右位置的所有深度神经元连接

- **边数与参数计算**：
  | L | depth | 核心节点 | 边数 | 总参数 |
  |---|-------|----------|------|--------|
  | 3 | 2 | 12 | 36（shared_x） / 48（full_x） | 49（shared_x） / 61（full_x） |
  | 4 | 1 | 10 | 26 | 37 |
  | 4 | 2 | 20 | 40（shared_x） / 64（full_x） | 61（shared_x） / 85（full_x） |
  - 公式：
    - `core_nodes = L(L+1)/2 × d`
    - `edges(shared_x) = d²×L(L-1)/2 + d×L(L-1) + 2Ld`
    - `edges(full_x) = 3d²×L(L-1)/2 + 2Ld`
  - 总参数 = 边数 + 核心节点 + 1（输出偏置）

- **可视化改进**：
  - 权重图标题显示 `L` 和 `depth`
  - Loss 曲线使用 log scale
  - 仅可视化里层（x=1），避免深度扩展时图形过密

- **文档更新**：
  - 更新 `readme.md` 和 `CLAUDE.md` 为 XYZ 坐标系统
  - 明确禁止 X 方向连接（同一位置内神经元不直接相连）
  - `depth` 从 list 改为 int（所有层使用相同深度）

- **实现验证**：
  - `depth=1, L=4`：10 核心节点，26 条边，37 参数
  - `depth=2, L=4`：20 核心节点，40 条边（shared_x） / 64 条边（full_x）
  - `depth=2, L=3`：12 核心节点，36 条边（shared_x） / 48 条边（full_x）
  - 训练测试通过（sin_mix 任务，10000 epoch）

---

## 2026-04-11

### 跨层连接模式切换加入

- 在 `params.yaml` 中加入 `cross_layer_mode`
- 默认值是 `shared_x`，保持旧版本行为不变
- 新增 `full_x` 模式，数学规则是：
  - `(*, y, z) -> (*, y, z+1)` 在 `x` 维上全连接
  - `(*, y, z+1) -> (*, y+1, z)` 在 `x` 维上全连接
- 这次升级的意义不是改层内结构，而是让两种跨层三角方向也可以像层内横向一样，在 `x` 维上形成位置到位置的全连接块
- 研究上这使得用户可以直接比较：
  - 旧版 `shared_x`：跨层只保留同 `x` 通道
  - 新版 `full_x`：跨层显式混合不同 `x` 通道

### 研究阶段切换：从“实现 TMN-3D”转到“理解 depth 版 TMN 的行为”

- 当前研究对象已经不是最早的标量 TMN，而是 **带 `depth` 的 TMN-3D**
- 当前参数文件显示，最近实验已经在尝试：
  - `L=3`
  - `depth=3`
  - `task_name=piecewise`
  - `mlp_layers=[4,8,4]`
  - `node_activation=leaky_relu`
  - `epochs=20000`
- 这意味着研究重点已经开始从“能否训练”转向：
  - `depth` 是否真的提升表示能力
  - 结构上是否出现可解释的分工
  - TMN 和 MLP 的差异是否稳定可重复

### 实验组织方式更新

- `runs/` 现在按日期归档：
  - `runs/20260409/...`
  - `runs/20260410/...`
  - `runs/20260411/...`
- 单次实验目录目前会生成两张核心图：
  - `comparison_4panel.png`
  - `weights.png`
- `comparison_4panel.png` 现在会直接写出网络架构：
  - TMN：`L`, `depth`, 参数量
  - MLP：`layers=[...]`, 参数量

### 最新 piecewise 现象：顶端衰减、底部稳固

- 最新 `piecewise` run：
  - `runs/20260411/piecewise_20260411_122344/`
- 从 `weights.png` 看，出现了一个重要现象：
  - 三角形顶端顶点附近参数明显衰减，接近 0
  - 下层、尤其底层附近参数逐渐稳定，并保持更大的绝对值
- 这与 TMN 的原始叙事高度一致：
  - 顶部 = 短路径 / 快速路线
  - 底部 = 长路径 / 长期部分 / 更稳定的慢路线
- 对 `piecewise` 这种目标，模型似乎更愿意把容量留给底部长期部分，而主动弱化顶部快路径

### 对这个现象的当前解释（暂定）

- 一个合理解释是：`piecewise` 需要更稳定、更分段的表示，因此浅层快路径不够，模型自动偏向深路径
- 另一个解释是：这部分现象可能混合了激活函数效应（尤其 `ReLU` / `leaky_relu` 下的节点存活性）
- 因此现在最值得做的不是再加新结构，而是验证：
  - 顶端衰减是否跨 seed 稳定出现
  - 这是不是 `piecewise` 特有现象
  - `depth` 和激活函数分别贡献了什么

### 代码层面当前值得注意的现实状态

- `run.py`、`train.py`、`plot.py` 都已经比最早版本复杂得多，说明项目进入了“活跃试验期”
- `readme.md` 和真实行为之间已经开始出现轻微漂移
- `timeline.md` 现在应该承担的不只是“功能完成记录”，更重要的是：
  - 记录当前结构版本
  - 记录关键实验现象
  - 记录下一步该验证什么

---

## 待办事项 (TODO)

### 已完成 ✓
- [x] **3D TMN 实现**：支持 `depth` 参数，每个位置可有多个神经元
- [x] **XYZ 坐标系统**：`(x,y,z)` = `(深度，位置，层)`
- [x] **连接规则**：层内 (Y)、下→上 (Z+)、上→下 (Z-) 三种连接
- [x] **可视化**：
  - `comparison_4panel.png`
  - `weights.png`
  - 图标题显示 TMN / MLP 架构和参数量
- [x] **按日期归档 runs**：`runs/YYYYMMDD/<experiment>/`

### 高优先级
- [ ] **修通主流程**：确保 `run.py`、`train.py`、`plot.py` 的接口始终一致，不再出现入口漂移
- [ ] **Depth 消融实验**：固定 `L=3`，系统比较 `depth=1 / 2 / 3 / 4`
- [ ] **连接模式消融**：固定 `L` 和 `depth`，比较 `cross_layer_mode=shared_x / full_x`
- [ ] **种子稳定性**：顶端衰减、底部强化是否在多个 seed 下重复出现
- [ ] **任务稳定性**：在 `sin_mix / piecewise / poly_wave` 上分别检查这种结构分工是否稳定出现

### 中优先级
- [ ] **激活函数对比**：`relu` vs `leaky_relu`，区分结构分工和 dead activation
- [ ] **参数归档**：每个 run 文件夹自动保存一份实际使用的 `params.yaml`
- [ ] **层级统计**：自动输出顶层/中层/底层参数范数，不只靠肉眼看图
- [ ] **benchmark 路线**：从函数拟合过渡到更能体现多时间尺度结构的任务

### 低优先级 / 未来方向
- [ ] **每层不同 depth**：`depth: list[int]` 支持（当前是统一 depth）
- [ ] **更大规模实验**：`L=8, depth=4` 等更高参数量设置
- [ ] **记忆任务设计**：设计真正能体现 TMN 长短路径差异的任务

---

## Dead Ends

### 2026-04-09 — `torch.compile` 优化失败
- **尝试**：用 `torch.compile(TMNNetwork)` 编译整个 forward
- **结果**：CPU 上反而更慢（编译开销 > 收益），速度从 0.465ms 退步到 0.554ms
- **原因**：`torch.compile` 针对 GPU/大矩阵优化，TMN 是小模型 + CPU，dispatch 开销占主导

### 2026-04-09 — 曲线图例设计
- **尝试**：用 `(src_row, src_col)->(dst_row, dst_col)` 作为边权标签
- **问题**：当 src 是 input 节点时（如 `("in", 1)`），索引 `[1]` 会越界
- **修复**：特判 input 节点，标签改为 `in,{src[1]}`

---

## 实验记录

| 日期 | 任务 | L | TMN val loss | MLP val loss | 备注 |
|------|------|---|--------------|--------------|------|
| 2026-04-09 | sin_mix | 4 | 0.0186 | 0.0070 | 第一次完整训练 + 参数演化图 |
| 2026-04-09 | sin_mix | 8 | 0.0307 | 0.0180 | L=8 时 TMN 更差，死亡节点更多 |
| 2026-04-09 | piecewise | 8 | 0.112 | 0.106 | piecewise 比 sin_mix 更难拟合 |
